# s21_string+ — Реализация библиотеки string.h с расширениями

## 📋 Содержание
1. [Обзор](#обзор)
2. [Функциональность](#функциональность)
3. [Сборка и установка](#сборка-и-установка)
4. [Использование](#использование)
5. [Тестирование и покрытие](#тестирование-и-покрытие)
6. [Совместимость](#совместимость)
7. [Структура проекта](#структура-проекта)
8. [Лицензия](#лицензия)

## 🔍 Обзор

**s21_string+** — это кроссплатформенная реализация стандартной библиотеки C `string.h` с дополнительными функциями для форматированного ввода/вывода (`sprintf`, `sscanf`) и обработки строк. Проект разработан в соответствии со стандартом C11 и обеспечивает поведение, идентичное оригинальным функциям, с поддержкой macOS и Linux.

## ⚙️ Функциональность

### ✅ Стандартные функции string.h

| Функция | Описание |
|---------|----------|
| `s21_memchr` | Поиск символа в блоке памяти |
| `s21_memcmp` | Сравнение блоков памяти |
| `s21_memcpy` | Копирование блоков памяти |
| `s21_memset` | Заполнение блока памяти |
| `s21_strncat` | Конкатенация строк с ограничением |
| `s21_strchr` | Поиск символа в строке |
| `s21_strncmp` | Сравнение строк с ограничением |
| `s21_strncpy` | Копирование строк с ограничением |
| `s21_strcspn` | Длина начального сегмента без указанных символов |
| `s21_strlen` | Длина строки |
| `s21_strpbrk` | Поиск первого вхождения любого символа |
| `s21_strrchr` | Поиск последнего вхождения символа |
| `s21_strstr` | Поиск подстроки |
| `s21_strtok` | Разделение строки на токены |

### ✨ Спецификаторы форматирования (sprintf/sscanf)

| Категория | Спецификаторы | Поддержка |
|-----------|--------------|-----------|
| **Целые числа** | `%d`, `%i`, `%u`, `%o`, `%x`, `%X` | ✅ Полная |
| **Числа с плавающей точкой** | `%f`, `%e`, `%E`, `%g`, `%G` | ✅ Полная |
| **Символы и строки** | `%c`, `%s` | ✅ Полная |
| **Указатели** | `%p` | ✅ Полная |
| **Счётчик символов** | `%n` | ✅ Полная |
| **Процент** | `%%` | ✅ Полная |

### 🎯 Расширения библиотеки

| Функция | Описание |
|---------|----------|
| `s21_to_upper` | Преобразование строки в верхний регистр |
| `s21_to_lower` | Преобразование строки в нижний регистр |
| `s21_insert` | Вставка подстроки в указанную позицию |
| `s21_trim` | Удаление указанных символов из начала и конца строки |
| `s21_strerror` | Получение текстового описания кода ошибки |

## 🛠 Сборка и установка

### Требования
- GCC/Clang с поддержкой C11
- Make
- Check framework (для тестирования)
- Lcov и genhtml (для отчётов о покрытии)

### Команды сборки

```bash
# Сборка статической библиотеки
make all

# Сборка и запуск тестов
make test

# Сборка с покрытием кода
make gcov_test

# Генерация отчёта о покрытии
make gcov_report

# Очистка артефактов сборки
make clean
```

### Структура сборки
- **Библиотека:** `s21_string.a`
- **Исполняемый файл тестов:** `test_string`
- **Отчёт о покрытии:** `coverage_report/`

## 📝 Использование

### Базовый пример

```c
#include "s21_string.h"
#include <stdio.h>

int main() {
    char buffer[100];
    
    // Форматированный вывод
    s21_sprintf(buffer, "Привет, %s! Число: %d", "мир", 42);
    printf("%s\n", buffer);  // Привет, мир! Число: 42
    
    // Форматированный ввод
    const char *input = "123 45.67";
    int a;
    float b;
    s21_sscanf(input, "%d %f", &a, &b);
    printf("a = %d, b = %.2f\n", a, b);  // a = 123, b = 45.67
    
    // Расширения
    char *upper = s21_to_upper("hello");
    printf("%s\n", upper);  // HELLO
    free(upper);
    
    return 0;
}
```

### Поддержка модификаторов

```c
// Модификаторы длины
short s;
long l;
long double ld;
s21_sscanf("123", "%hd", &s);   // short
s21_sscanf("123", "%ld", &l);   // long
s21_sscanf("123.45", "%Lf", &ld); // long double

// Флаги форматирования
char buffer[50];
s21_sprintf(buffer, "%+05d", 42);  // "+0042"
s21_sprintf(buffer, "%-10s", "left"); // "left      "
s21_sprintf(buffer, "%#x", 255);    // "0xff"
```

## 🧪 Тестирование и покрытие

### Запуск тестов

```bash
# Обычные тесты
make test

# Тесты с покрытием
make gcov_test

# Генерация HTML-отчёта
make gcov_report
```

### Структура тестов
- **Unit-тесты:** Check framework
- **Сравнительное тестирование:** Проверка идентичности поведения с оригинальными функциями
- **Краевые случаи:** NULL-указатели, пустые строки, переполнение буфера
- **Кроссплатформенное тестирование:** macOS и Linux

## 💻 Совместимость

### Платформы
- **macOS** (Darwin) — полностью поддерживается
- **Linux** — полностью поддерживается

### Стандарты
- **Язык:** C11 (`-std=c11`)
- **Компиляторы:** GCC, Clang
- **Флаги:** `-Wall -Wextra -Werror -pedantic`

## 📁 Структура проекта

```
s21_string+/
├── s21_string.h          # Заголовочный файл, типы, структуры
├── s21_string.c          # Реализация стандартных функций
├── s21_sprintf.c         # Реализация форматированного вывода
├── s21_sscanf.c          # Реализация форматированного ввода
├── s21_strerror.c        # Обработка кодов ошибок
├── s21_sharp.c           # Расширения (to_upper, to_lower, insert, trim)
├── test_s21_string.c     # Модульные тесты
├── Makefile              # Система сборки
└── README.md             # Документация
```

## ⚠️ Особенности реализации

### Форматированный ввод/вывод
- Полная поддержка спецификаторов `%d`, `%i`, `%u`, `%o`, `%x`, `%X`, `%f`, `%e`, `%E`, `%g`, `%G`, `%c`, `%s`, `%p`, `%n`
- Поддержка флагов: `-`, `+`, пробел, `0`, `#`
- Поддержка ширины поля и точности
- Поддержка модификаторов длины: `h`, `l`, `L`
- Обработка специальных значений: `inf`, `nan`, `(nil)`

### Управление памятью
- Все выделения памяти используют `malloc`/`free`
- Расширения (`s21_to_upper`, `s21_insert`, `s21_trim`) требуют освобождения памяти
- Нулевое завершение всех строк гарантировано

### Кроссплатформенность
- Адаптация сообщений об ошибках для macOS и Linux
- Размер типов определяется архитектурой (x86_64, ARM64)
- Поддержка многобайтовых символов через `wchar_t`

---

<div align="center">
<i>Учебный проект</i><br>
</div>